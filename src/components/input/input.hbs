<input
    class="input"
    type="{{type}}"
    name="{{name}}"
    value="{{value}}"
    placeholder="{{placeholder}}"
    data-validation="{{validation}}"
>
<label class="label-err" for="{{name}}">
  {{errorText}}
</label>
